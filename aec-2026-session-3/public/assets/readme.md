# API Deployment

You can deploy the API and Leaderboard locally using `docker compose up`. The API will be available at `http://localhost:3000`. The leaderboard will be available at `http://localhost:80`.
